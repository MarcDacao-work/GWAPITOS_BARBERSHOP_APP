// This file is an archive of the previous HomeScreen implementation.
// It was moved to HomeScreen_OLD.js so the app can use separate
// `CustomerHomeScreen` and `BarberHomeScreen` components instead.
// Keep this file for reference. Remove permanently if not needed.

import React from 'react';
import { View, Text } from 'react-native';

const ArchivedHomeScreen = () => (
  <View style={{flex:1,justifyContent:'center',alignItems:'center'}}>
    <Text>Archived HomeScreen - not used.</Text>
  </View>
);

export default ArchivedHomeScreen;
